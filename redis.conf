# Redis configuration for queue workloads
# Set eviction policy to noeviction to prevent data loss in job queues
maxmemory-policy noeviction

# Enable append-only file for persistence
appendonly yes

# Other recommended settings for reliability
# maxmemory 256mb  # Uncomment and adjust if needed
# tcp-keepalive 300
# timeout 0